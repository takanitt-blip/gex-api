name: Update GEX History

# ─────────────────────────────────────────────────────────────
# v19: 誤判断36 本体修正 ── cron を生成境界直後（ET 未明）へ移行
# ─────────────────────────────────────────────────────────────
# 背景（PC_GOVERNANCE 誤判断36 / PC_PIPELINE §5.10）:
#   旧 cron（UTC 21:47 = ET 17:47/18:47、引けと同日の夕方）は、
#   as_of がまだ「引けた当日」を指す時間帯に発火していた。この時点
#   では greeks/eod は未生成（生成完了は引けから約9時間後）なため、
#   rest.py の _resolve_trade_date は安全側に倒れて as_of の「前営業日」
#   を返す設計になっており、結果として本来使えるはずの最新1営業日分の
#   鮮度を毎回失っていた。
#
#   2026-07-04〜07-18 の実測キャンペーン（tools/probe_eod_availability.py）
#   で、greeks/eod の生成完了は引けから 8h57m〜9h10m 後（月〜木は
#   ET 00:57〜00:59、金曜のみ ET 01:10 と外れる、n=5）と確定した。
#
#   これを受けて cron を「引けの翌未明」へ移動する。as_of が既に
#   「引けの翌暦日」を指す時間帯に発火すれば、_resolve_trade_date の
#   ロジック（as_of の前日から遡る）自体は無変更のまま、正しく
#   「直近の引けた取引日」を安全に取得できる（rest.py 側の docstring
#   にも同じ説明を明記済み）。
#
# 変更点 (v18 → v19):
#   - cron: '47 21 * * 1-5' (UTC 21:47) → '30 6 * * 2-6' (UTC 06:30)
#     曜日も 1-5(月-金) → 2-6(火-土) にずらしている点に注意。
#     「月〜金の引け」に対応する取得は「火〜土の未明」に発火する
#     必要があるため（土曜の発火が金曜分の取得を担当する）。
#     UTC 06:30 は EST(冬季) で ET 01:30、EDT(夏季) で ET 02:30 相当。
#     金曜最遅実測(ET 01:10)からのマージンは冬季 20分・夏季 1h20m。
#     このマージンを使い切るケースは run_daily.py 側の収束待ち
#     リトライ（デッドライン ET 06:45）で吸収する設計。
#   - run_daily.py: get_option_chain が空でも即座に諦めず、
#     ET 06:45 まで5分間隔でリトライする（誤判断36 対応、
#     run_daily.py 側のモジュール docstring 参照）。
#   - ヘルスチェック URL の expiration/date を実行日基準の動的値に変更
#     （obs.K: 固定日付 2026-06-18 / 20260512 が陳腐化するリスクへの
#     予防的対応。tools/probe_eod_availability.yml で先行導入済みの
#     「実行日の30日前」方式を踏襲）。
#   - timeout-minutes を明示。冬季 cron 起点(ET01:30)からデッドライン
#     (ET06:45)までの最大待機幅は約5h15m=315分。セットアップ/後片付けの
#     余裕を見て 340 分に設定（GitHub Actions 既定の 360 分上限に収める）。
#
# ─────────────────────────────────────────────────────────────
# v18: snapshot → history 全面移行に伴う修正
# ─────────────────────────────────────────────────────────────
# rest.py が snapshot から history エンドポイントへ全面再設計された
# (DESIGN_history_rest_adapter.md)。これに伴う yml 側の修正:
#   - ヘルスチェックの URL を snapshot/open_interest から
#     history/greeks/eod に変更（後述、誤判断16 の教訓）
#   - cron 根拠コメントから snapshot キャッシュリセットの記述を削除
# cron 時刻 (UTC 21:47) 自体は変更不要（理由は下記）。
#
# ─────────────────────────────────────────────────────────────
# 段階 6D (v17.4): cron 時刻の高負荷帯回避調整
# ─────────────────────────────────────────────────────────────
# 段階 6D で変更したもの (v17.3 → v17.4):
#   - cron 時刻を UTC 22:30 → UTC 21:47 に変更
#
# 変更理由 (運用観察に基づく):
#   v17.3 で設定した UTC 22:30 は GitHub Actions の混雑帯
#   (UTC 22:00〜00:30、世界中の cron が UTC 00:00 周辺に集中) に
#   位置していたため、3 営業日連続で約 10 時間のキュー滞留が
#   発生していた (Run #36/#37/#38 で UTC 08:12〜09:34 実発火を観測)。
#
#   公式コミュニティの言明:
#     "Schedules that run during periods of high load
#      (e.g., midnight UTC) may be delayed or, if load is
#      sufficiently high, potentially dropped."
#
#   設計上の取得可能窓:
#   history/greeks/eod は ET 17:15 に生成される確定 EOD レポートを返す。
#   snapshot と違い深夜キャッシュリセットの概念は無く、いつ叩いても
#   同じ値が返る（実行時刻非依存、DESIGN_history_rest_adapter.md 1）。
#   さらに rest.py は取引日 T を「前営業日」に倒す設計のため、当日 EOD
#   の生成タイミングにも依存しない。
#   したがって cron 時刻の制約は「GitHub Actions の混雑帯を避ける」
#   ことだけ。この観点で混雑帯を外した時刻として UTC 21:47 を選定。
#
#   分を 47 にしたのは「キリの良い数字 (0/15/30/45) は
#   他のジョブと衝突しやすい」という GitHub Actions コミュニティの
#   定石に従ったもの。
#
# 段階 6D 完了判定 (v17.3 から継続):
#   2 営業日連続成功 (曜日依存・夜間ジョブの安定性確認)
#
# 段階 6C 初回実行で発覚した問題と修正履歴 (v17.1 / v17.2):
#   v17.1: --creds-file creds.txt を引数に明示
#   v17.2: jar URL を v3 用に変更、ヘルスチェックを本番エンドポイントに変更
#
# ─────────────────────────────────────────────────────────────

on:
  # ── v19 で cron を生成境界直後（ET 未明）へ移行（誤判断36 本体修正）──
  # 曜日は 2-6（火-土）。月〜金の引けに対応する取得はそれぞれ翌暦日の
  # 未明に発火する必要があるため（土曜の発火が金曜分を担当）。
  schedule:
    - cron: '30 6 * * 2-6'   # UTC 06:30 (ET 01:30 EST / 02:30 EDT)
  workflow_dispatch:           # 手動実行も引き続き可能（デッドライン超過時のバックアップ用途にも使用）

permissions:
  contents: write              # gex_history.json を commit & push するため

jobs:
  build:
    runs-on: ubuntu-latest
    # v19: 冬季 cron 起点(ET01:30)からデッドライン(ET06:45)までの最大
    # 待機幅は約5h15m=315分。セットアップ/後片付けの余裕を見て340分に
    # 設定（GitHub Actions 既定の360分上限に収める。詳細は冒頭コメント）。
    timeout-minutes: 340
    steps:
      # ───── 環境構築 ─────
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '25'

      - name: Verify Java version
        run: java -version

      # ───── Theta Terminal セットアップ ─────
      - name: Download Theta Terminal v3 jar (from official URL)
        # 公式ドキュメント (https://docs.thetadata.us/Articles/Getting-Started/Getting-Started.html)
        # が指す v3 専用 URL を使用。
        #
        # ⚠️ 注意: download-latest.thetadata.us は v2 系列 (v1.8.6) を返すため
        #         使えない。v3 用の URL は別系統 (download-unstable.thetadata.us)。
        #         「unstable」という名称だが、公式ドキュメントが指している
        #         現時点の v3 公式 DL URL である。
        run: |
          curl -L -o ThetaTerminalv3.jar https://download-unstable.thetadata.us/ThetaTerminalv3.jar
          ls -lh ThetaTerminalv3.jar

      - name: Configure creds.txt
        # secrets.THETA_CREDS には改行区切りで
        #   1 行目: メールアドレス
        #   2 行目: パスワード
        # を登録しておく前提
        run: |
          printf '%s\n' "${{ secrets.THETA_CREDS }}" > creds.txt
          # creds.txt の中身は出力しない（パスワード保護）
          # 行数のみ確認
          wc -l creds.txt

      - name: Start Theta Terminal in background
        # nohup でバックグラウンド起動、ログは theta_terminal.log に書き出す
        # 起動失敗時のデバッグのため、ログは後段で必ず確認する
        #
        # ★ v17.1 修正 (2026-05-10):
        #   --creds-file creds.txt を明示的に指定する。
        #   公式の起動仕様では「同じディレクトリに creds.txt を置く」だけでは
        #   自動認識されず、引数で明示する必要がある。
        #   段階 6C 初回実行時に "No line found" エラーで起動失敗したため、
        #   公式 usage に従って明示指定する形に修正。
        run: |
          nohup java -jar ThetaTerminalv3.jar --creds-file creds.txt > theta_terminal.log 2>&1 &
          echo "Started Theta Terminal with PID $!"
          echo "$!" > theta_terminal.pid

      - name: Wait for Theta Terminal health check
        # 最大 60 秒、1 秒ごとに本番エンドポイントを叩いて 200 を待つ。
        #
        # ⚠️ v17.2 修正 (2026-05-10):
        #   旧 yml では /v3/stock/list/symbols（Stock: FREE で叩ける軽量
        #   エンドポイント）を使っていたが、これが偽陽性の原因だった。
        #   段階 6B-2 で v2 jar が偶然 stock 系のヘルスチェックを通し、
        #   段階 6C で初めて Option 系の REST API が叩けないと判明した。
        #   （誤判断16: ヘルスチェック = 本番で使うエンドポイントが
        #     叩けることの確認であるべき）
        #
        # ⚠️ v18 修正 (snapshot → history 移行):
        #   rest.py が history 化され、本番は snapshot を一切叩かなくなった。
        #   ヘルスチェックも本番が叩く history エンドポイントに合わせる。
        #   合わせないと「snapshot は叩けるが history 系で 471 PERMISSION」
        #   を見逃し、誤判断16 の偽陽性が再発する。
        #
        #   採用: /v3/option/history/greeks/eod。
        #     理由: history 化後の本番の中核（IV 健全性フィルタの本丸）。
        #           Options: STANDARD 権限が無いと 471 を返すため、
        #           プラン権限の確認にもなる。
        #     軽量化: expiration を 1 満期に絞る。expiration=* は
        #             36 満期 約 6 MB を返し、60 回ループで重い。
        #     date 固定: start_date=end_date=20260512 は「ヘルスチェック
        #             専用の固定過去営業日」。本番の取得日とは無関係で、
        #             権限と疎通の確認が目的。history なので過去日付の
        #             データは安定して返る。
        #
        #   ⚠️ 実装後検証項目 → obs.K で決着 (v19):
        #     固定 expiration=2026-06-18 は満期後も history/greeks/eod が
        #     過去満期の過去データを問題なく返し続けることを実データで
        #     確認済み（gex_history.json の 07/01・07/02・07/06 エントリが
        #     満期経過後も正常生成）。ただし固定値は将来また陳腐化が
        #     心配になる書き方であり、tools/probe_eod_availability.yml で
        #     先行導入済みの「実行日の30日前」動的計算方式に合わせる
        #     （予防的対応、obs.K）。
        #
        #   curl の -w "%{http_code}" は接続失敗時も "000" を出力するので、
        #   || echo "000" は不要（付けると "000000" の二重出力になる）。
        run: |
          set +e  # curl 失敗時に即座に exit しない
          # v19: 固定日付ではなく実行日基準（UTC、30日前）で動的計算。
          # expiration・start_date・end_date とも同じ過去日でよい
          # （疎通と権限確認が目的で、本番の取得日とは無関係）。
          HEALTHCHECK_DATE=$(date -u -d '30 days ago' +%Y%m%d)
          HEALTHCHECK_EXPIRATION=$(date -u -d '30 days ago' +%Y-%m-%d)
          HEALTHCHECK_URL="http://127.0.0.1:25503/v3/option/history/greeks/eod?symbol=SPY&expiration=${HEALTHCHECK_EXPIRATION}&start_date=${HEALTHCHECK_DATE}&end_date=${HEALTHCHECK_DATE}&format=csv"
          echo "Healthcheck date: ${HEALTHCHECK_DATE} (expiration=${HEALTHCHECK_EXPIRATION})"
          for i in $(seq 1 60); do
            status=$(curl -s -o /dev/null -w "%{http_code}" "$HEALTHCHECK_URL" 2>/dev/null)
            if [ "$status" = "200" ]; then
              echo "Theta Terminal is ready (took ${i} seconds)"
              exit 0
            fi
            echo "Attempt ${i}/60: HTTP $status, waiting..."
            sleep 1
          done
          echo "❌ Theta Terminal did not become ready within 60 seconds"
          echo "── theta_terminal.log (last 50 lines) ──"
          tail -50 theta_terminal.log || true
          exit 1

      - name: Show Theta Terminal startup log (for confirmation)
        # 起動ログを Actions ログに出力（バージョン番号や Subscription 状態を確認）
        # PROJECT_CONTEXT v16 で「実 API 環境で踏みやすい 3 つの地雷
        # (471 PERMISSION / 476 WRONG_IP / 478 多重起動)」を後から検証するため
        run: |
          echo "── theta_terminal.log (head 30 lines) ──"
          head -30 theta_terminal.log

      # ───── Python 依存と検証 ─────
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: python -m pytest gex_engine/tests/ -q

      # ───── REST 駆動で daily run（段階 6C の本題）─────
      - name: Run daily GEX update (REST)
        # ★ 段階 6C で mock → rest に切替
        # ThetaRestAdapter が Theta Terminal (localhost:25503) 経由で
        # 実 SPY のオプションチェーンを取得し、GEX を計算する。
        # 失敗時は run_daily.py 内で sys.exit(1) → このステップが赤になる。
        env:
          GEX_DATA_SOURCE: rest
        run: python -m gex_engine.scripts.run_daily

      # ───── 結果コミット ─────
      - name: Commit gex_history.json if changed
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add gex_history.json
          git diff --quiet && git diff --staged --quiet \
            || (git commit -m "Auto-update GEX history (rest, stage 6D)" && git push)

      # ───── 後片付け (任意) ─────
      - name: Stop Theta Terminal
        # ジョブ終了時に runner ごと破棄されるので必須ではないが、
        # 明示的に停止することで「Terminal が動作していた」事実を確認できる
        if: always()
        run: |
          if [ -f theta_terminal.pid ]; then
            pid=$(cat theta_terminal.pid)
            kill "$pid" 2>/dev/null || true
            echo "Stopped Theta Terminal (PID $pid)"
          fi
